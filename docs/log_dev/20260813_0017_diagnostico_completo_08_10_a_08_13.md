# 20260813_0017_diagnostico_completo_08_10_a_08_13 — Diagnóstico Profundo rodado até o fim, cobrindo os dias que faltavam

Log de Máquina (Regra 10.3 — append-only). Continuação do RELATÓRIO
20260813_0016 (fix do volume de impressão): faltava uma captura completa
do relatório, cobrindo `08-10` a `08-13` — os dias mais próximos do
sintoma originalmente relatado, que nunca tinham sido capturados.

## O que foi feito

1. `fix/distancia-fallback-sincronizacao` foi mesclada em
   `fix/imc-perfil-atleta1000` (commit `67df081`) antes desta rodada, pra
   que o app testado no device físico já tivesse o fallback
   `_preencherDistanciaFaltante`.
2. Fundador desbloqueou o device físico e navegou até "Histórico de
   Telemetria" logado como `atleta1000@teste.com` — confirmado por
   screenshot via `adb exec-out screencap` antes de prosseguir.
3. Buffer do `adb logcat` limpo (`logcat -c`), fundador apertou "GERAR
   LOG DIAGNÓSTICO (30 DIAS)".
4. Buffer capturado via `adb logcat -d` (sem depender do terminal do
   fundador) logo em seguida — relatório parecia cortado no meio do dia
   `2026-08-12`, sem marcador de FIM. Esperados 20s e recapturado: desta
   vez com o marcador `FIM DO RELATÓRIO` presente. Confirma que mesmo
   pós-fix do RELATÓRIO 20260813_0016, o `debugPrint` ainda tem um
   pequeno atraso de drenagem — a UI "termina" antes do buffer de log
   terminar de escoar. Não é mais o bug de 33+ minutos, é uma folga de
   segundos; próxima vez, esperar ~20-30s depois do retorno da UI antes
   de capturar.

## Resultado

- Relatório completo do início ao fim: `21:14:09` a `21:14:37` (**~28
  segundos**, contra 33+ minutos do bug anterior).
- Cobriu os 30 dias inteiros, `2026-07-14` a `2026-08-13` (hoje incluído
  — `1255` pontos brutos, `9` tipos diferentes).
- **Zero avisos `⚠️` de distância com soma zero em qualquer um dos 30
  dias**, incluindo os 4 pedidos:
  - `08-10`: `DISTANCE_DELTA` 5 ponto(s)
  - `08-11`: `DISTANCE_DELTA` 1 ponto(s)
  - `08-12`: `DISTANCE_DELTA` 3 ponto(s)
  - `08-13`: `DISTANCE_DELTA` 5 ponto(s)
- `WEIGHT`/`BODY_FAT_PERCENTAGE` só aparecem em 4 dos 30 dias (`07-28`,
  `07-29`, `08-01`, `08-10`) — reforça a conclusão do RELATÓRIO
  20260813_0014 (pesagem real esporádica, não falha de sincronização;
  sem pesagem nova desde `08-10`).

## Não resolvido / próximo passo

O sintoma original (distância/peso "sumindo" em dias específicos) **não
reproduziu** nesta janela de 30 dias — nem nos dias novos (`08-10` a
`08-13`) nem nos antigos. Isso não prova que o sintoma nunca aconteceu
(o Health Connect pode ter recebido dados atrasados do Garmin depois do
fato, como já registrado no RELATÓRIO 20260813_0016 pra `07-26`/`07-29`),
mas não há mais nenhuma pista nova pra perseguir sem: 1) uma nova
ocorrência relatada em tempo real pelo fundador, capturada com
`adb logcat -d` no mesmo dia (não dias depois); ou 2) considerar a
investigação de causa-raiz encerrada por ora e focar em builds/push da
branch `fix/imc-perfil-atleta1000` (que já acumula fixes reais e
testados: IMC, catch silencioso, fallback de distância, e a
instrumentação de diagnóstico).
