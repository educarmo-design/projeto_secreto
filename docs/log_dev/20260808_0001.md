# Log de Dev — 20260808_0001

**Tarefa:** N17 (delta diário) + N18 (Carga Inicial 30 dias) — persistência
idempotente de telemetria de wearables em `metricas_saude_diarias`.

**Branch:** `feature/n17-n18-telemetria-persistida`.

- **Arquivos alterados:**
  - `android/app/src/main/AndroidManifest.xml` — 2 permissões novas (READ_RESTING_HEART_RATE, READ_LEAN_BODY_MASS).
  - `lib/features/dashboard/data/models/health_payload_model.dart` — campos `frequenciaCardiaca` e `massaMagraKg`.
  - `lib/features/dashboard/data/services/health_sync_service.dart` — `carregarHistoricoInicial` passa a persistir (bug corrigido); `_lerEGravar` extraído e compartilhado com `sincronizarDeltaDiario`; anomalia de FC agora checa `frequencia_cardiaca`.
  - `lib/features/dashboard/presentation/controllers/sync_ui_controller.dart` — `conectarWearablePelaPrimeiraVez()` novo, compartilha `_executar` com `forcarSincronizacaoAtleta`.
  - `lib/features/dashboard/presentation/pages/main_navigation_page.dart` — dispara sync automático em `initState` (N17 "ao abrir o app").
  - `lib/features/dashboard/presentation/pages/registrar_metrica_page.dart` — botão "conectar wearable" agora persiste via `SyncUiController`.
  - `supabase/migrations/20260808120000_metricas_saude_fc_massa_magra.sql` — colunas `frequencia_cardiaca`, `massa_magra_kg`. Aplicada no remoto.
  - 2 arquivos de teste atualizados/estendidos (`health_sync_service_test.dart`, `sync_ui_controller_test.dart`).
- **Entidades novas:** colunas `metricas_saude_diarias.frequencia_cardiaca` e `.massa_magra_kg`; método `HealthSyncService._lerEGravar`; método `SyncUiController.conectarWearablePelaPrimeiraVez`.
- **Estado das branches:** `feature/n17-n18-telemetria-persistida` commitada (1 commit), não mesclada em `main` — aguardando validação do fundador no celular. Migration já aplicada no banco remoto (independe do merge do código).
- **Riscos:**
  - Sync automático em `initState` pode disparar mais de uma vez por sessão se `MainNavigationPage` for recriada (redirects do router) — inofensivo, `sincronizarDeltaDiario` é idempotente, só gera chamadas extras ao Health Connect.
  - Coluna `frequencia_cardiaca` nova fica retroativamente vazia para dados já sincronizados antes desta tarefa (sem backfill possível — o dado bruto por tipo não fica retido após o merge).
  - Sem teste HTTP/dispositivo real nesta tarefa — só testes automatizados (54 verdes) e build debug local. Teste físico é o critério de aceite do fundador.
