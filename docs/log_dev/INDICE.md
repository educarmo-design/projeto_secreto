# Índice — Log de Desenvolvimento

Governança de logs de desenvolvimento (N25, Parte 10.3 do Mestre). Cada entrada é
um relatório de fim de tarefa, um arquivo por tarefa, nomeado `AAAAMMDD_NNNN.md`
(NNNN = sequencial do dia, começando em 0001).

| Arquivo | Data | Resumo |
|---|---|---|
| [20260807_0001.md](20260807_0001.md) | 2026-08-07 | Auditoria do estado do Git (N26): F10 Passo 3 + F34 + D2-PII já estavam mesclados na main desde 2026-07-30; build debug confirmado sem erros; 24 commits publicados em origin/main e as 3 branches obsoletas removidas (local+remoto) por autorização do fundador; inicialização deste log. |
| [20260807_0002.md](20260807_0002.md) | 2026-08-07 | N20: re-seed de embeddings reais em `alimentos_referencia` (637 linhas, não ~8.000 como o Mestre registrava), índice HNSW, secrets `EMBEDDING_MODEL_NAME`/thresholds, correção de bug de `taskType` ausente em `search-food`. |
| [20260808_0001.md](20260808_0001.md) | 2026-08-08 | N17+N18: persistência idempotente de telemetria de wearables — corrige `carregarHistoricoInicial` que só lia e nunca gravava (Carga Inicial de 30 dias), liga o delta diário automático ao abrir o app, adiciona `frequencia_cardiaca`/`massa_magra_kg`. |
| [20260809_0001.md](20260809_0001.md) | 2026-08-09 | Corrige bug "só 2 dias" da Carga Inicial (causa raiz: permissão `READ_HEALTH_DATA_HISTORY` ausente, restrição do próprio Health Connect) + N19: tela de Histórico de Telemetria com botões de debug "FORÇAR SYNC HOJE"/"FORÇAR CARGA 30 DIAS". |
| [20260810_0001.md](20260810_0001.md) | 2026-08-10 | Corrige valores divergentes do Garmin: passos/calorias ativas via agregado nativo do Health Connect (evita double-counting entre fontes), minutos de sono sem somar SLEEP_SESSION+SLEEP_ASLEEP em dobro, frequência cardíaca vira média aritmética do dia (era "última leitura"). |
| [20260810_0002.md](20260810_0002.md) | 2026-08-10 | Corrige bug de fuso (o agregado nativo da entrada anterior fatiava o dia em UTC, não local — passos/sono errados de novo); abandona `getHealthIntervalDataFromTypes`, volta pra leitura crua com janela alinhada à meia-noite local; sono granular por estágio (leve/profundo/REM/acordado, decisão de produto) com migration nova. |
| [20260810_0003.md](20260810_0003.md) | 2026-08-10 | Spike (sem mutação de código): mapeamento dos `HealthDataType` de FC e balança/composição corporal disponíveis no pacote `health` no Android; registro formal do backlog "Motor de Sono Avançado" (não implementado na v1.0). |
| [20260810_0004.md](20260810_0004.md) | 2026-08-10 | `fc_maxima` (máximo bruto de FC do dia), `agua_corporal` e `imc` (direto ou inferido peso/altura²) em `metricas_saude_diarias`; inferência cruzada massa magra ↔ percentual de gordura; nova coluna `perfis_usuarios.altura_cm`. Restrição regulatória (proibição de detecção de anomalia de FC, pertence ao Motor Clínico F02/Backlog) preservada — achado pré-existente que já viola a restrição foi documentado no código, não removido unilateralmente. |
| [20260810_0005.md](20260810_0005.md) | 2026-08-10 | Corrige "permissão negada" no teste físico: faltava `READ_BODY_WATER_MASS` e `READ_HEIGHT` no AndroidManifest (BODY_MASS_INDEX não existe como permissão nativa — o pacote `health` substitui por WEIGHT+HEIGHT no Android). Corrige `fc_maxima` nunca aparecendo na tela: campo nunca tinha sido adicionado a `HealthPayloadModel`, não só à UI. Tela de histórico passa a exibir FC Máxima/HRV/Água corporal/IMC. |
| [20260810_0006.md](20260810_0006.md) | 2026-08-10 | Corrige double-counting de distância (celular+relógio somavam em vez de pegar a maior fonte, mesmo bug já corrigido para passos/calorias). Nova Tela de Perfil do Usuário (`perfil_usuario_page.dart` + `perfil_usuario_repository.dart`) para editar `altura_cm` pelo app em vez de SQL manual, com navegação a partir da aba Perfil já existente. |
| [20260810_0007.md](20260810_0007.md) | 2026-08-10 | Hierarquia de Fontes: passos e distância passam a vir SEMPRE da mesma "fonte vencedora" do dia (pedômetros nativos Google Fit/Samsung Health/iPhone despriorizados), cross-plataforma via `sourceId`(iOS)/`sourceName`(Android). Corrige resiliência do IMC histórico (falha transitória na busca de altura não apaga mais o lote de 30 dias inteiro). Projeto nunca teve suporte a iOS — gerado agora (`flutter create --platforms=ios`) + `NSHealthShareUsageDescription`/`NSHealthUpdateUsageDescription` no Info.plist; não verificável neste ambiente (Windows, sem Xcode). |
| [20260811_0001.md](20260811_0001.md) | 2026-08-11 | Corrige "upsert destrutivo": `_enviarLinhas` mandava o lote inteiro (até 30 dias) num único `.upsert()`, e o PostgREST enchia com `NULL` explícito qualquer coluna ausente numa linha quando outra linha do mesmo lote a tinha — raiz comum de "dados de ontem apagados", "distância/calorias sumindo em dias aleatórios" e "IMC não calcula". Corrigido: um `.upsert()` por linha. Modo Raio-X (deep logging cru por dia/fonte antes de qualquer agregação). Spike exaustivo de Nutrição/Atividades (WORKOUT/WORKOUT_ROUTE/NUTRITION/calorias) com diferenças Android×iOS documentadas. |
| [20260811_0002.md](20260811_0002.md) | 2026-08-11 | Sincronização de Treinos: nova `atividades_fisicas_treinos`/`atividades_fisicas_rotas`/`tipos_atividades_fisicas` (dicionário), FC de cada treino calculada filtrando HEART_RATE bruto pelo intervalo exato do treino, rota GPS gravada com idempotência "limpa e reinsere", `READ_BASAL_METABOLIC_RATE` no Android (WORKOUT_ROUTE não precisa de permissão nova — consentimento por sessão do próprio Health Connect). Calorias granulares (ativas/basais/totais) em `metricas_saude_diarias` e na tela. Nova `historico_treinos_page.dart`. |
| [20260811_0003.md](20260811_0003.md) | 2026-08-11 | Spike somente-leitura: mapeamento do Painel Web (React, não Flutter Web) e sua RBAC; auditoria da criptografia D2 (pgcrypto+Vault, não pgsodium) — achado de bug: `AdminDashboard.tsx` lia PII ciphertext cru pós-D2; planejamento arquitetural prévio do Domínio N06. |
| [20260811_0004.md](20260811_0004.md) | 2026-08-11 | Spike somente-leitura: auditoria de conformidade N01-N05/D3 — N01 (RBAC) era só 2 booleans, não M:N; N02 (CRM/CRN) tinha coluna mas nunca era coletado/exibido; N03 (18+) não existia em nenhuma ponta; N04 (vínculo/consentimento) era o domínio mais maduro; N05 (telefone) nunca era coletado no cadastro; D3 (matriz de permissões) não existia como artefato algum. |
| [20260811_0005.md](20260811_0005.md) | 2026-08-11 | Fecha N01-N05 (N03 nas 3 pontas: CHECK constraint + Painel Web + App Flutter) e implementa D3 como RBAC dinâmico M:N no banco (`papeis`/`permissoes`/`papeis_permissoes`/`usuario_papeis` + RPCs, sem nenhum artefato `.md` estático) com tela `AdminMatrizPermissoes.tsx` de gestão em tempo real. Módulo N06: catálogos (`alergias`, `configuracoes_sistema`), RPC D2 `admin_perfis_seguro` (corrige o bug de ciphertext cru do spike anterior) e 6 novas telas administrativas no Painel Web. |
| [20260811_0006.md](20260811_0006.md) | 2026-08-11 | N06 completo: reverte por instrução do fundador a trava de escrita de `alimentos_referencia`/`alimentos_medidas_caseiras` — `AdminAlimentos.tsx` vira CRUD real (modal com macros + porções). Novas RPCs D2 `admin_listar_vinculos`/`admin_encerrar_vinculo`/`admin_aprovar_vinculo` (replicam a regra de negócio da Edge Function `manage-professional-link`) e nova tela `AdminVinculos.tsx`. `AdminAtividadesFisicas`/`AdminAlergias` ganham edição inline; `AdminConfiguracoes` ganha remoção; `AdminUsuarios`/`AdminProfissionais` ganham alteração de `status_aprovacao` e gestão de papéis (D3) via `PapeisEditor.tsx` compartilhado. Zero toque em `lib/` (Flutter). |
| [20260811_0007.md](20260811_0007.md) | 2026-08-11 | N09: Anamnese Nutricional Versionada self-service — trigger `anamneses_trg_versionar` (verificado funcionalmente com usuário de teste real) garante no máximo 1 anamnese `ativo` por usuário; 3 tabelas N:N (`anamneses_alergias`/`anamneses_problemas_saude`/`anamneses_atividades`, esta com `minutos_diarios`); novo catálogo `problemas_saude` + `tipos_atividades_fisicas.met_estimado` (gaps N06/N07). Painel Web: `AdminProblemasSaude.tsx` (novo CRUD) + campo MET em `AdminAtividadesFisicas.tsx`. App Flutter: novo `lib/features/nutricao/` com `anamnese_self_service_page.dart` (Objetivo/Problemas de Saúde/Alergias/Rotina de Atividades). |
| [20260812_0008.md](20260812_0008.md) | 2026-08-12 | N07: RPC `calcular_motor_metabolico` (TMB Katch-McArdle/Mifflin-St Jeor completa, PAL decomposto = gasto_sedentario + gasto_atividade da anamnese ativa, TEF isolado nunca somado ao tdee) — verificada com 3 casos reais e conferência manual da matemática. Sexo biológico: achado de auditoria (coluna já existia como `text` livre com 10 linhas de seed em português) convertido para ENUM `sexo_biologico_enum` com migração segura dos dados existentes; editável no app (RadioGroup) e no Painel Web (`PatientDetails.tsx`, via view + RPC `profissional_atualizar_sexo_biologico`). Telemetria manual: `InserirMedicaoModal.tsx` com proteção de sobrescrita (SELECT antes do upsert, confirmação explícita) e novas policies de escrita profissional-vinculado em `metricas_saude_diarias`. |
| [20260812_0009.md](20260812_0009.md) | 2026-08-12 | Tela de simulação do Motor Metabólico no Painel Web: novo `MotorMetabolicoCard.tsx` (TMB/Gasto Sedentário/Gasto de Atividade/TDEE em destaque, TEF isolado explicitamente não somado, fórmula usada, insumos, avisos de dado faltante), montado em `PatientDetails.tsx` com recálculo automático ao salvar medição ou trocar sexo biológico. Só consome a RPC `calcular_motor_metabolico` já verificada em 20260812_0008 — nenhuma migration nova. |
| [20260812_0010.md](20260812_0010.md) | 2026-08-12 | N08 (Motor de Exceções de dupla via) + N10 (Prescrição Profissional) + N11 (Meta Self-Service): nova tabela `objetivos_alimentares` (sem policy de escrita — só a RPC grava) + trigger de versionamento por `(usuario_id, tipo_dia)` + RPC `validar_e_salvar_meta` (chama o Motor N07 internamente; profissional só recebe avisos, atleta é bloqueado por trava clínica ANVISA, prioridade B2B do profissional, ou carência de 30 dias) — verificada com 9 asserções funcionais reais. Painel Web: `PrescricaoView.tsx` (cards, formulário, "Copiar Último", banner de avisos) em `PatientDetails.tsx`. App Flutter: `meta_bem_estar_page.dart` (2 estados de bloqueio de tela inteira + modal vermelho de trava clínica) e `meta_bem_estar_repository.dart`. |
| [20260812_0011_fix_imc.md](20260812_0011_fix_imc.md) | 2026-08-12 | Fix: IMC não calculava para `atleta1000@teste.com`. Causa raiz encontrada por consulta real ao banco: o usuário tinha `metricas_saude_diarias` cheia de dados do Garmin mas ZERO linha em `perfis_usuarios` — `atualizarAlturaCm`/`atualizarDataNascimento`/`atualizarSexoBiologico` usavam `.update()`, que "sucede" silenciosamente (0 linhas afetadas, sem exceção) quando a linha não existe. Trocado por `.upsert()` (cria a linha se faltar) — sem migration nova, a RLS já permitia. Adicionado IMC ao vivo (reativo, recalcula a cada dígito) em `perfil_usuario_page.dart`, já que nenhuma tela do app jamais calculava/mostrava IMC client-side. Fix reproduzido e confirmado contra o banco real com um usuário no mesmo estado quebrado. |
| [20260812_0013_fix_distancias.md](20260812_0013_fix_distancias.md) | 2026-08-12 | Investigação de distância nula em dias específicos para `atleta1000@teste.com`: auditoria de banco confirma `distancia_metros IS NULL` de verdade em 11 dos últimos 34 dias (não só os 5 reportados), sem correlação com dia da semana/volume de passos; auditoria de código (sincronização + UI) não encontra nenhum bug de aplicação — distância é tratada de forma idêntica/simétrica a `passos` (que nunca falha) em bucketização, unidade e Hierarquia de Fontes. Mitigação defensiva aditiva `_preencherDistanciaFaltante`: segunda leitura estreita (só tipos de distância) best-effort, preenche só dias que ficaram faltando sem nunca sobrescrever um dia já resolvido — sem risco de contagem em dobro. |
| [20260813_0014_fix_regression_telemetria.md](20260813_0014_fix_regression_telemetria.md) | 2026-08-13 | Auditoria de regressão reportada ("peso/gordura pararam de sincronizar após o fix do IMC"): descartada — o diff da correção de IMC não tocou `health_sync_service.dart` em nenhuma linha, `WEIGHT`/`BODY_FAT_PERCENTAGE` continuam pedidos, `.upsert()` de `metricas_saude_diarias` já loga erro real. Achado real (pré-existente, não causado pelo fix do IMC): `_lerComPermissao` tinha um `catch (_)` que engolia qualquer exceção sem log, abortando o lote inteiro em silêncio — corrigido para logar o erro real, mesmo comportamento best-effort preservado. Consulta ao banco mostra a última pesagem real em 2026-08-10 (peso/gordura/massa magra consistentes entre si) — sem pesagem nova desde então, não uma falha de sync; `passos` seguiu chegando normalmente nos dias sem pesagem, o que descarta o achado do catch como causa do sintoma relatado. |
| [20260813_0015_instrumentacao_telemetria.md](20260813_0015_instrumentacao_telemetria.md) | 2026-08-13 | Modo de Diagnóstico Profundo. Parte 1: proteção try/catch por ponto em 3 lugares que convertiam listas inteiras de uma vez — **achado real, confirmado por teste**: `_detectarEregistrarAnomalias` chamava `toPayload()` sem proteção, DENTRO de `_lerComPermissao`; um único ponto com valor NaN/Infinity (ex.: STEPS) derrubava o LOTE INTEIRO (todos os dias/tipos) como "permissão negada" — mecanismo mais plausível pro sintoma "distância/calorias/peso/treinos sumindo em dias aleatórios". Parte 2: logs `[SYNC_DIAGNOSTICO]` verbosos (contagem por tipo/dia, valor bruto + runtimeType de cada ponto, limites exatos startTime/endTime, dump bruto quando distância soma 0 com pontos presentes) — desligados por padrão, só ligam sob demanda. Parte 3: novo `executarDiagnosticoProfundo` + botão "GERAR LOG DIAGNÓSTICO (30 DIAS)" em `historico_telemetria_page.dart`. Aguardando saída real de device físico do fundador para confirmar a hipótese em produção. |
| [20260813_0016_fix_volume_diagnostico.md](20260813_0016_fix_volume_diagnostico.md) | 2026-08-13 | Fundador rodou o botão de diagnóstico (20260813_0015) num device físico e "não apareceu nada" — lido o buffer real via `adb logcat -d` (sem depender do terminal do fundador): o relatório rodou (41.446 pontos em 30 dias), mas `HEART_RATE` sozinho tem 23.437 pontos (leitura contínua, até 956/dia) e o detalhe ponto a ponto de TODO tipo, sem exceção, fazia o `debugPrint` (limitador de taxa embutido) levar 33+ minutos só pra imprimir 26 dos 30 dias. Corrigido: tipos de leitura contínua (HEART_RATE, HRV, estágios de sono) agora só imprimem contagem, nunca detalhe ponto a ponto; demais tipos ganham teto de segurança de 50 pontos detalhados. Achado bônus, não conclusivo: `2026-07-26`/`2026-07-29` (2 das 5 datas do RELATÓRIO 20260812_0013) já têm distância no Health Connect hoje, apesar do banco ter `NULL` na época — indício a favor de atraso de sincronização do Garmin. |
| [20260813_0017_diagnostico_completo_08_10_a_08_13.md](20260813_0017_diagnostico_completo_08_10_a_08_13.md) | 2026-08-13 | `fix/distancia-fallback-sincronizacao` mesclada em `fix/imc-perfil-atleta1000` (commit `67df081`) antes da rodada. Botão de diagnóstico (20260813_0015/0016) rodado de novo num device físico, capturado via `adb logcat -d` com um reforço de ~20s de espera pós-retorno da UI (o `debugPrint` ainda tem folga de drenagem de segundos, não mais os 33+ min do bug anterior) — relatório completo do início ao fim em ~28s, cobrindo os 30 dias inteiros inclusive `08-10` a `08-13` (os dias que nunca tinham sido capturados). Resultado: **zero avisos `⚠️` de distância zerada em qualquer dia**, incluindo os 4 pedidos — todos com `DISTANCE_DELTA` presente e não-nulo. `WEIGHT`/`BODY_FAT_PERCENTAGE` só em 4 dos 30 dias, reforçando que é pesagem esporádica real, não falha de sync. Sintoma original não reproduziu nesta janela — investigação de causa-raiz sem pista nova sem uma ocorrência relatada em tempo real. |
| [20260813_0018_fix_hierarquia_fontes_distancia.md](20260813_0018_fix_hierarquia_fontes_distancia.md) | 2026-08-13 | Causa-raiz real encontrada (fundador reportou distância ainda ausente na tela): `_pedometrosNativos` era lista EXATA (só Google Fit/Samsung Health/Apple Health) e não reconhecia `com.android.healthconnect.phone.<hash>` — o próprio Health Connect contando passos pelo acelerômetro do aparelho, sem app terceiro. Essa fonte entrava em prioridade ALTA na "Fonte Vencedora" e podia vencer o Garmin só por ter mais passos brutos; como nunca reporta distância, o dado real do Garmin era descartado em silêncio (confirmado com `2026-08-11` do RELATÓRIO 20260813_0017). Corrigido: `_ehPedometroNativo` agora casa por prefixo (`_prefixosPedometrosNativos`), não só lista exata. Teste de regressão novo confirma Garmin com menos passos mas com distância vencendo a fonte sem distância. 89+166 testes passando, `flutter analyze` limpo. Ainda não chegou no device físico (precisa rebuild). |
| [20260813_0019_fix_calorias_basais_fonte_wearable.md](20260813_0019_fix_calorias_basais_fonte_wearable.md) | 2026-08-13 | Fundador reportou `calorias_basais` só aparecendo em dias com pesagem, e definiu (conforme especificação) que calorias devem vir do Garmin, nunca do Fitdays. Causa raiz: `BASAL_ENERGY_BURNED` bruto só existia no Health Connect nos MESMOS 4 dias com `WEIGHT` — Fitdays só calcula essa estimativa no instante exato da pesagem (mesmo timestamp, mesma fonte), nunca mede continuamente; Garmin nunca reportou `BASAL_ENERGY_BURNED` nenhuma vez nos 30 dias. `aplicarMaiorFonte` (calorias) não tinha noção de prioridade de fonte, então a balança sempre "vencia" por ser a única. Corrigido: novo `_ehFonteValidaParaCalorias` exclui `cn.fitdays.fitdays` (+ reaproveita `_ehPedometroNativo` pras suítes de celular) da agregação de calorias — diferente da Hierarquia de passos/distância, calorias NUNCA caem pra fonte excluída (fica `null`, nunca vira estimativa de app de balança). 2 testes de regressão novos, 91+168 testes passando, `flutter analyze` limpo. Ainda não chegou no device físico. |
| [20260819_0020_verificacao_device_fixes_0018_0019_e_achado_total_calories.md](20260819_0020_verificacao_device_fixes_0018_0019_e_achado_total_calories.md) | 2026-08-19 | Rebuild + instalação dos fixes 0018/0019 no device físico (primeira vez). Achado operacional: botões de debug da tela de Histórico exigem app em primeiro plano até o snackbar — background no meio trava a gravação sem log de erro. Fixes 0018 (distância) e 0019 (calorias basais) CONFIRMADOS via consulta direta ao banco (service role) após zerar (41 linhas) e recarregar do zero o histórico de `atleta1000`: zero avisos de distância zerada, `calorias_basais` virou `null` nos 7 dias que antes usavam estimativa do Fitdays. Divergência de passos (15/08, 16/08) vs app do Garmin investigada e atribuída ao Health Connect vs sync tardio do Garmin, fora do nosso controle — decisão do fundador: manter como está. Achado novo, não implementado: `HealthDataType.TOTAL_CALORIES_BURNED` (mapeado pro `TotalCaloriesBurnedRecord` nativo do Health Connect) nunca foi solicitado nem tem permissão Android declarada — candidato forte pra calorias totais contínuas direto do Garmin, já que o Garmin tem permissão de escrita confirmada pra esse tipo. Pendente de decisão de desenho antes de implementar. |
| [20260819_0021_fix_treino_permissao_total_calories_e_novas_metricas_garmin.md](20260819_0021_fix_treino_permissao_total_calories_e_novas_metricas_garmin.md) | 2026-08-19 | Modo autônomo, sem device (fundador usando o celular). CAUSA RAIZ real do bug "treino não aparece na tela nem grava no banco" (stack trace confirmada): o pacote `health`, ao processar `WORKOUT`, lê `TotalCaloriesBurnedRecord` por baixo dos panos pra cada sessão — sem `READ_TOTAL_CALORIES_BURNED` no Manifest, `SecurityException` derrubava a leitura do WORKOUT inteiro (não era bug de parsing Dart). Corrigido + implementadas as métricas pedidas: `TOTAL_CALORIES_BURNED` (calorias totais direto do wearable, com fallback ativas+basais preservado), `FLIGHTS_CLIMBED` (andares subidos, maior fonte do dia), `SPEED` (velocidade média/máxima por treino, mesmo tratamento de FC). Migration nova (`andares_subidos`, `velocidade_media_ms`/`velocidade_maxima_ms`) aplicada no banco remoto — achado no caminho: histórico de migrations local desalinhado do remoto (`20260812230000_seed_problemas_saude_clinicos.sql` só existia em `feat/seed-problemas-saude`, nunca mesclada), trazido antes do push. `ELEVATION_GAINED` (ganho de elevação) investigado e não implementável: pacote `health` 13.3.1 não expõe esse tipo. Investigação adicional de passos/distância 15/08-16/08: nenhum bug novo encontrado, reafirma conclusão anterior. `flutter analyze` limpo; 98 (+7) testes em `health_sync_service_test.dart`, 180 (+12) no total de `test/features/dashboard/`, tudo verde. 11 falhas pré-existentes e não relacionadas em `test/features/nutrition/` (fora do escopo, registradas pra visibilidade). Nada verificado em device físico ainda. |
| [20260820_0001_n16_hidratacao.md](20260820_0001_n16_hidratacao.md) | 2026-08-20 | N16 (Documento Mestre v7.0, fila Fase 4-5) — Hidratação, escolhida pelo fundador entre as opções do backlog. Modo autônomo, sem device. Registro em ml reaproveita `coleta_diaria` (EAV, `atributo='agua_ml'`, sem tabela nova); tamanho do copo configurável (padrão 200 ml) vira coluna nova `perfis_usuarios.tamanho_copo_ml` (migration aplicada no banco remoto). Nova `RegistroHidratacaoPage` (total do dia, "+1 copo", quantidade avulsa, histórico de 7 dias, editar tamanho do copo) + botão na AppBar do Dashboard + novo 9º widget configurável (`DashboardWidgetId.hidratacao`/`HidratacaoCard`) reaproveitando a Fábrica de Widgets já existente. i18n pt/en/es completo. `flutter analyze` limpo; 24 testes novos (8 `coleta_diaria_repository_test.dart` novo, 6 `perfil_usuario_repository_test.dart`, 10 `registro_hidratacao_page_test.dart` novo), suíte inteira sem regressão (mesmas 11 falhas pré-existentes de `confirmacao_prato_page_test.dart`, não relacionadas). Nada verificado em device físico ainda. |
| [20260820_0002_fix_treino_forca_e_solucao_definitiva_fuso_horario.md](20260820_0002_fix_treino_forca_e_solucao_definitiva_fuso_horario.md) | 2026-08-20 | Fundador testou de verdade e reportou: treino de força nunca aparece (corrida/Pilates ok) — causa raiz achada: `tipos_atividades_fisicas` só tinha tipos "Both" (comuns Android+iOS), e treino de força nunca é "Both" (`STRENGTH_TRAINING`/`WEIGHTLIFTING` Android-only, `FUNCTIONAL`/`TRADITIONAL_STRENGTH_TRAINING` iOS-only) — FK rejeitava silenciosamente. Corrigido, migration aplicada. Distância/calorias erradas em 13-17/08: hipótese inicial errada (corrigida publicamente pelo próprio fundador em tempo real) descartada; causa raiz real com evidência direta — a recarga completa de 30 dias do RELATÓRIO 20260819_0020 rodou com o relógio do aparelho em Lima (`adb shell date` capturado nesta mesma sessão confirma `-05`), reprocessando o histórico inteiro (inclusive dias anteriores à viagem) sob o fuso errado. 20/08 explicado como "dia em andamento" (app do Garmin atualiza ao vivo), não bug. SOLUÇÃO DEFINITIVA implementada por decisão do fundador ("é um produto, todo usuário que viajar vai passar por isso"): histórico de transições de fuso próprio do app (secure storage), bucketização por dia passa a usar o fuso REAL de cada ponto (resolvido individualmente, não por sincronização) em vez do fuso atual do sistema — elimina a classe inteira do bug dali pra frente, para qualquer usuário, permanentemente; não corrige retroativamente (impossível, Health Connect não expõe fuso original por ponto). 5 linhas de `metricas_saude_diarias` (13-17/08) removidas do banco, ficam em branco até recarga futura sob fuso compatível. `flutter analyze` limpo; 105 (+7) testes em `health_sync_service_test.dart`, 348 no total da suíte, mesmas 11 falhas pré-existentes não relacionadas. Nada verificado em device físico (fundador em viagem). |
| [20260821_0001_card_consumo_meta_e_n15_aparelho_por_foto.md](20260821_0001_card_consumo_meta_e_n15_aparelho_por_foto.md) | 2026-08-21 | Modo autônomo (fundador dormindo, autorização ampla). Fila de backlog: 2 de 4 itens pedidos. **Card consumo × meta (N12)**: novo `ColetaDiariaRepository.buscarConsumoHoje()` (soma `valor_jsonb->totais` das refeições de hoje) + `MetaBemEstarRepository.buscarMetaEfetivaAtual()` (mesma precedência profissional>própria já usada em `MetaBemEstarPage`) + 10º widget na Fábrica já existente (`ConsumoMetaCard`) — calorias/macros consumidos vs. meta, barra de progresso, sem duplicar a tela de definir meta. **N15 aparelho por foto**: achado real — o diálogo de resultado (`showExtractedDataDialog`) nunca gravava nada, só exibia; renomeado pra `mostrarDialogoConfirmarLeituraAparelho`, agora persiste em `coleta_diaria` via `gravarLeituraAparelho` (mesmo padrão de refeição/água) e mostra snack de sucesso/erro; as 3 telas que abrem o diálogo atualizadas. `flutter analyze` limpo; +14 testes novos (repositórios + diálogo), 359 no total da suíte, mesmas 11 falhas pré-existentes não relacionadas. Nada verificado em device (fundador dormindo). Próximo: favoritas (N13) + editar unidade de medida. |
| [20260821_0002_n13_favoritas_e_fix_critico_confirmacao_prato.md](20260821_0002_n13_favoritas_e_fix_critico_confirmacao_prato.md) | 2026-08-21 | Modo autônomo (fundador dormindo). Últimos 2 dos 4 itens do backlog. **Achado**: "editar unidade de medida" (N12) já estava pronto (categorias liquido_frio/liquido_quente/unidade/fatia/peso_livre cada uma com edição própria) — Documento Mestre desatualizado, não implementei nada redundante. **Favoritas (N13)**: nova tabela `alimentos_favoritos` (migration aplicada), `FavoritasRepository`, botão ⭐ em `ConfirmacaoPratoPage` salva o payload revisado (com edições já feitas) como favorita, nova `FavoritasPage` cobre escolher+usar E gerenciar (trocar tipo/excluir) na mesma tela, entrada em `ConfiguracoesPerfilPage`. **FIX CRÍTICO achado no caminho**: as "11 falhas pré-existentes" de `confirmacao_prato_page_test.dart`, carregadas em TODOS os relatórios desta semana como "não relacionadas", eram na verdade o arquivo INTEIRO quebrado (confirmado isolando numa worktree limpa do HEAD) — bug real de produção: `Flexible` como filho direto de `Wrap` em `_ItemPratoTile` (combinação inválida no Flutter) derrubava a tela de confirmar refeição toda vez que um item renderizava. Corrigido (fix mínimo, remove o `Flexible`) + 1 teste desatualizado corrigido (esperava um texto "Identificado como:" que a UI real nunca produz — chave i18n órfã, sobra de redesign anterior). `flutter analyze` limpo; 16 testes novos (favoritas_repository +9, favoritas_page +7) + 3 novos em confirmacao_prato_page_test.dart; **suíte inteira: 389 testes, ZERO falhas — primeira vez 100% verde nesta sessão inteira**. Nada verificado em device físico (fundador dormindo). |
| [20260823_0001_resultado_testes_device_e_gaps_favoritas.md](20260823_0001_resultado_testes_device_e_gaps_favoritas.md) | 2026-08-23 | Fundador testou tudo em device físico real. ✅ OK: correção de fuso horário, treino de força, hidratação (N16), glicosímetro/balança gravando em `coleta_diaria` (N15). ⚠️ Card consumo×meta (N12): aparece na tela, cálculo ainda não validado pelo fundador (verificação pendente, não é bug reportado). ⚠️ Favoritas (N13): 2 gaps reais encontrados pelo fundador — (1) criar uma favorita só existe no botão ⭐ de `ConfirmacaoPratoPage`, a tela de Favoritas em si não tem como criar; (2) nunca existiu edição de CONTEÚDO de uma favorita salva (só trocar tipo/excluir). Fundador pediu pra trabalhar nos 2 gaps — implementação no próximo relatório desta sequência. |
| [20260823_0002_fix_gaps_criar_e_editar_favoritas.md](20260823_0002_fix_gaps_criar_e_editar_favoritas.md) | 2026-08-23 | Fecha os 2 gaps de Favoritas do RELATÓRIO 20260823_0001. **Criar do zero**: novo FAB "+" em `FavoritasPage` abre `CriarFavoritaPage` (busca manual de alimentos + monta itens com macros por 100g, gramas ajustáveis) — dá um chamador real ao `search-food`/`FoodSearchController`, até então órfão. **Editar conteúdo**: `ConfirmacaoPratoController` ganha `aoConfirmar` (override do destino de `confirmar()`, preserva toda a máquina de estado); `ConfirmacaoPratoPage` ganha modo `FavoritaEmEdicao` (título/botão mudam, ⭐ some); `FavoritasRepository.atualizarPayload` novo; `FavoritasPage` ganha "Editar itens" no menu, reabrindo a favorita via `PratoRefeicaoExtracaoModel.fromJson(payload_jsonb)` — reaproveita 100% da tela de edição de itens do F10 Passo 3, zero duplicação. Diálogo "nome+tipo" extraído de `confirmacao_prato_page.dart` pra `DialogoNomeTipoFavorita` (público, reaproveitado pelos dois pontos de criação). `flutter analyze` limpo; +19 testes novos (2 repositório, 4 controller, 3 página de confirmação em modo edição, 3 página de favoritas, 7 página de criar) + 1 teste existente corrigido (texto do empty state); suíte inteira: 407 testes, ZERO falhas. Nada verificado em device físico ainda — recomendado confirmar os 2 fluxos novos no próximo teste real. |
| [20260823_0003_investigacao_medidas_caseiras_categoria_consumo.md](20260823_0003_investigacao_medidas_caseiras_categoria_consumo.md) | 2026-08-23 | Investigação pura (sem mudança de código), pedida pelo fundador antes de reportar um bug — mapeia a relação `alimentos_referencia`×`alimentos_medidas_caseiras` (1:N por FK), onde as duas são usadas em React (`web_painel/AdminAlimentos.tsx`) e Flutter (`ConfirmacaoPratoPage`, `CriarFavoritaPage`, `ManualFoodSearchPage`), e confirma que não existe coluna "revisão" no banco — o fluxo real de revisão é externo/offline via CSV gerado por script. **Achado de bug real**: a resposta HTTP de `extract-metric-photo` (câmera) NUNCA inclui `categoria_consumo`/`unidade_medida_padrao`/`medida_padrao_nome`/`medida_padrao_qtd` — a interface `ItemPratoCalculado` no servidor nem carrega esses campos adiante (só são lidos dentro de `encontrarMedida` pra montar o texto do fallback, e morrem ali). Resultado: `item.categoriaConsumo` chega sempre `null` no Flutter, então a UI condicional de líquido frio/quente/unidade/fatia em `ConfirmacaoPratoPage._buildContenudoEstimado` é código morto — 100% das fotos caem no fallback genérico de peso livre (rotulado em gramas mesmo para líquidos em ml), nunca na UI especializada por categoria. Documentado com exemplo concreto (fotografar café) e citações exatas de arquivo/linha. Sem fix ainda — aguardando o relatório do fundador com a reformulação pedida da tela/processo de cadastrar refeição. |
| [20260823_0004_correcao_catalogo_alimentos_e_categoria_consumo.md](20260823_0004_correcao_catalogo_alimentos_e_categoria_consumo.md) | 2026-08-23 | Modo autônomo (3 decisões confirmadas com o fundador antes de começar). **Migration** `20260823100000`: `revisao_necessaria`/`observacao_revisao` novos em `alimentos_referencia` E `alimentos_medidas_caseiras`. **Curadoria em massa** (novo script `web_painel/scripts/curar_catalogo_alimentos_ia.ts`, Gemini em lote): dos 637 alimentos TACO/USDA, aliases vazios caiu de 594→0, categoria/unidade nulos de 620→0, alimentos SEM nenhuma medida caseira de 599→0 (270 linhas genéricas sem sentido apagadas e regeneradas: 1.056 linhas específicas por alimento — azeitona→unidade 3g, presunto→fatia 15-20g, coxinha→unidade 30-160g, conforme pedido); 9 alimentos + 12 medidas marcados `revisao_necessaria=true` com observação real (nenhuma falha técnica). **Fix do achado 20260823_0003**: `extract-metric-photo/index.ts` agora propaga `categoria_consumo`/`unidade_medida_padrao`/`medida_padrao_nome`/`medida_padrao_qtd` até a resposta HTTP (antes morriam em `encontrarMedida`) — deployado; regra de QUANDO mostrar a UI especializada no Flutter mantida como estava (decisão confirmada). Achados extras corrigidos ao rodar `deno check` pela primeira vez limpo nesta função: `LinhaAlimentoBruta` sem os 4 campos, guard de null ausente em `resolverComBuscaSemantica`. **Reformulação `AdminAlimentos.tsx`**: lista+detalhe com 2 blocos de manutenção separados (dados TACO no topo, medidas caseiras embaixo), cada um com filtro próprio de "precisa de revisão"; `database.ts` (tipos do painel) ganhou as 6 colunas que faltavam — causa raiz de por que a tela antiga nunca expunha esses campos. Verificação só ao final (pedido do fundador): Deno 80/84 (4 falhas pré-existentes confirmadas via worktree isolado, não relacionadas), Flutter 407/407 sem regressão, `tsc -b`/eslint limpos no painel. |
| [20260824_0001_fix_performance_foto_prato_e_fila_revisao_catalogo.md](20260824_0001_fix_performance_foto_prato_e_fila_revisao_catalogo.md) | 2026-08-24 | Fundador confirmou catálogo populado, reportou 2 achados reais em device. **Fila de revisão discoverável**: 2 cards novos em `AdminOverview.tsx` (contagem de alimentos/medidas em revisão) + nova tela `AdminRevisaoCatalogo.tsx` (2 rotas: `/admin/revisao/alimentos` e `/admin/revisao/medidas-caseiras`) — lista com observação visível direto, marcar/desmarcar inline, some da fila ao desmarcar. **Lentidão/502 ao fotografar prato — causa raiz confirmada** (3 screenshots em `docs/bugs/`, `TimeoutException` 30s + `HTTP 502 "Falha ao analisar a imagem"`): (1) zero retry na chamada de visão do Gemini (modelo CORE `gemini-flash-latest`) — o MESMO 503 "high demand" batido pela curadoria em massa na véspera; corrigido com retry curto (2 tentativas, 1.5s/3s, orçamento pequeno de propósito — requisição síncrona de usuário, não script em lote). (2) leitura completa do catálogo (637 alimentos + 1.056 medidas, ~310KB/~1,5s medido) em TODA foto, sempre do zero; corrigido com cache em memória no escopo do módulo (TTL 5min, seguro entre usuários — dado público). Cliente Flutter: `_uploadTimeout` 30s→45s (dar folga pro retry do servidor). Deployado. Deno 82/84 (+2 testes novos do retry; mesmas 4 falhas pré-existentes não relacionadas), Flutter 407/407, `tsc`/eslint do painel limpos. |
| [20260824_0002_correcao_causa_raiz_cota_gemini_fallback_modelo.md](20260824_0002_correcao_causa_raiz_cota_gemini_fallback_modelo.md) | 2026-08-24 | Correção da correção anterior — fundador testou de novo e reportou "1ª foto ok, resto falha, demora mais" + "não é o Gemini, é a implementação". Estava certo: o RELATÓRIO 20260824_0001 diagnosticou 503 (instabilidade transitória) por analogia com a curadoria da véspera, mas o erro real de produção é **HTTP 429 RESOURCE_EXHAUSTED** — confirmado chamando o Gemini de verdade com a chave do projeto: modelo CORE (`gemini-flash-latest` → `gemini-3.7-flash`) no **free tier, limite de 20 requisições/DIA, já esgotado**; modelo LITE testado e funcionando (200 OK). Retentar em segundos (correção anterior) nunca ajuda uma cota diária — só fazia o usuário esperar mais pra ver a mesma falha, exatamente o sintoma relatado. **Correção real**: 429 nunca mais é retentado no mesmo modelo (`ErroCotaGemini`, falha imediata); novo `criarChamadorGeminiComFallback` degrada automaticamente CORE→LITE quando a cota do CORE esgota; mensagem de erro final honesta ("limite diário atingido", não "tente novamente") quando os dois esgotam. **Achado estratégico registrado**: fallback é mitigação, não solução — o produto tem um teto diário real de operações de IA enquanto `GEMINI_API_KEY` estiver no free tier; decisão de habilitar billing é do fundador, não resolve por código. Deploy feito. Deno 85/88 (+3 líquido; mesmas 4 falhas pré-existentes não relacionadas), Flutter inalterado (nenhum arquivo Dart tocado). |
| [20260824_0003_registro_refeicao_4_metodos.md](20260824_0003_registro_refeicao_4_metodos.md) | 2026-08-24 | Especificação completa do fundador (3 decisões confirmadas antes de começar): tela única com 4 métodos de registrar refeição — descritivo (texto livre → interpretação), áudio (nunca armazenado), favoritos (agora sempre com revisão antes de salvar, não mais 1 toque) e foto (já existente, sem mudança). Backend generaliza o pipeline "IA traduz, backend calcula" da foto pra também aceitar texto/áudio como fonte (`processarPratoRefeicao`/`FontePratoRefeicao`), reaproveitando 100% do casamento/cálculo determinístico já existente; texto e áudio no nível LITE (mais barato de cota). Flutter: `RegistroRefeicaoIaService`/`Controller` novos, `DescreverRefeicaoPage`/`GravarRefeicaoPage` (pacote `record`, Zero Storage — apaga o clipe assim que os bytes viram RAM) /`EscolherMetodoRefeicaoPage` (grade 2x2, o widget único pedido) novas, 11º widget do dashboard + botão na AppBar. **Achado de teste mais fundo da sessão**: um teste travava os 10 minutos de timeout mesmo após 3 causas plausíveis corrigidas — raiz real é `testWidgets` rodar dentro de uma zona `FakeAsync` onde I/O real do `dart:io` nunca completa (precisa de `tester.runAsync()`), e mesmo dentro dele `tester.pump(duration)` só avança um relógio FAKE sem ceder tempo real nenhum (precisa de `Future.delayed` de verdade entre pumps) — documentado em detalhe pra não repetir a investigação. `flutter analyze` limpo; Flutter 427/427 (zero regressão), Deno 91/95 (mesmas 4 falhas pré-existentes não relacionadas). Nada verificado em device físico ainda. |
| [20260825_0001_fix_4_testes_deno_desatualizados_fallback_medida.md](20260825_0001_fix_4_testes_deno_desatualizados_fallback_medida.md) | 2026-08-25 | Fundador pediu pra corrigir as 4 falhas Deno "pré-existentes, não relacionadas" carregadas desde 20260823_0004. **Não eram bug de produção** — eram 4 testes desatualizados: esperavam que `encontrarMedida` devolvesse `null` quando a medida pedida não bate com nenhuma cadastrada pro alimento, mas a função já não funciona assim desde o `FIX (31/jul)` documentado no próprio código — hoje sempre cai num fallback em camadas (primeira medida disponível → peso genérico → peso padrão da categoria) em vez de descartar o item; o comentário do RELATÓRIO 20260823_0004 já dizia que esse `null` "é inalcançável na prática", mas os testes nunca tinham sido atualizados pra bater. Corrigidos os 4 testes (viraram 6, cobrindo tanto o fallback real quanto o único caso residual de `null`, medida vazia) — zero mudança em `index.ts`. `deno check` limpo; **97/97 passou, zero falhas — primeira vez 100% verde nesta função**. Flutter inalterado (nenhum arquivo Dart tocado). |
| [20260825_0002_revisao_item_deep_link_e_catalogo_completo_atividades.md](20260825_0002_revisao_item_deep_link_e_catalogo_completo_atividades.md) | 2026-08-25 | Modo autônomo, enquanto o fundador testava fisicamente os 4 métodos de refeição. **Botão "Revisar item"**: cada linha da fila de revisão (`AdminRevisaoCatalogo.tsx`) ganhou um link deep-linkado pra `AdminAlimentos.tsx` (que já mostra alimento+observação+medidas caseiras juntos, RELATÓRIO 20260823_0004) via `?id=`/`&medida=` — sem duplicar UI; medida caseira nunca revisada isolada do alimento dono. **Catálogo completo de atividades físicas**: investigação programática (não visual, evita erro de contagem) do `HealthWorkoutActivityType` (pacote `health` 13.3.1) achou 99 códigos no total (43 Both + 35 iOS-only + 20 Android-only + OTHER) contra só 47 cadastrados — **52 faltando**, cada um capaz de reproduzir o mesmo bug silencioso do treino de força (RELATÓRIO 20260820_0002: FK rejeitada, engolida pelo `catch` best-effort). Migration `20260825100000` cadastra os 52 (fix não-hardcoded: o dicionário É a allowlist da FK, por desenho desde `20260811160000`) + coluna nova `plataforma` (documentação, não usada pela FK) pra nunca mais depender de reconferir o enum na mão. Verificação de integridade cruzando programaticamente o enum completo contra as 3 migrations de seed: zero código faltando, zero típo, zero duplicata — confirmado também contra o banco remoto real (99 linhas, 99 códigos únicos). `AdminAtividadesFisicas.tsx` ganhou coluna "Plataforma" (badge). `tsc -b`/eslint limpos no painel. Flutter inalterado. Nada verificado em device físico pro botão novo do painel ainda. |
| [20260825_0003_fix_timeout_foto_e_performance_registro_refeicao.md](20260825_0003_fix_timeout_foto_e_performance_registro_refeicao.md) | 2026-08-25 | Fundador testou os 4 métodos: texto/áudio funcionaram mas "lentos", foto deu `TimeoutException` (mesma classe já registrada em `docs/bugs/`). A screenshot anexada é comprovadamente de 24/08 (mensagem "0:00:30", timeout do cliente já era 45s desde 20260824_0001 — se fosse do build atual diria "0:00:45"), mas o relato ao vivo de hoje não foi descartado por isso. **Causa raiz real**: `criarChamadorGeminiComFallback` (20260824_0002) só trocava CORE→LITE em cota (429), nunca em 5xx esgotado — o CORE (foto) podia bater as 3 tentativas inteiras contra SI MESMO (cada uma uma chamada de visão completa) antes de desistir, facilmente estourando o timeout do cliente, e cada tentativa ainda queimava cota própria (free tier: só 20/dia). **Correção**: fallback dispara em QUALQUER falha do primário agora (não só cota); o primário ganha só 1 tentativa quando existe fallback configurado (o orçamento de retry cheio vai pro modelo que sobra); backoff reduzido de 1.5s/3s pra 1s/2s (chamada síncrona de usuário, não o script de curadoria em lote que justificou o valor original). Cache do catálogo 5min→30min (achado: é por isolate Deno, não compartilhado — 5min era curto demais pra "esquentar" de verdade, afeta os 3 métodos igualmente). Cliente: timeout 45s→60s (folga real pro pior caso "CORE falha uma vez + LITE esgota 3 tentativas"). "Áudio mais lento que texto" documentado como esperado (payload maior + modelo tem que ouvir antes de interpretar), não bug. `deno check` limpo, Deno 99/99 (4 testes de fallback reescritos/adicionados pro novo comportamento), `flutter analyze` 30 avisos pré-existentes sem relação, Flutter 17/17 nos arquivos afetados. Edge Function deployada. Nada verificado em device físico ainda. |
| [20260825_0004_investigacao_profunda_timeout_gemini_sem_alteracao_codigo.md](20260825_0004_investigacao_profunda_timeout_gemini_sem_alteracao_codigo.md) | 2026-08-25 | Fundador testou de novo após 20260825_0003: foto ainda timeout, texto ainda lento, áudio agora "servidor ocupado" — pediu investigação a fundo SEM nenhuma mudança de código ("relate não altere", perdeu confiança). **Zero arquivo de produção tocado** — só chamadas reais de diagnóstico (Gemini direto + a própria Edge Function já deployada, autenticada com usuário de teste descartável, deletado ao final). **Achado**: o fix de 20260825_0003 partiu de premissa errada — assumiu que um 503 volta rápido; medido ao vivo, o CORE (`gemini-flash-latest`) leva **45-59s só pra devolver o erro** (2 chamadas diretas ao Gemini). Pior: a Edge Function real, pedindo foto, levou **141,7s** pra responder (sucesso, mas lentíssimo); pedindo áudio, **150,8s até a própria Supabase matar a função com `WORKER_RESOURCE_LIMIT`** (não é erro do Gemini — é o teto de execução da plataforma, ~150s, não configurado por nós, sendo estourado porque o Gemini está tão lento que nem nossa função aguenta esperar). **Descartado como causa**: rotação de secrets da Supabase (achada no caminho, mas confirmada não-relacionada — trace do CLI não mostra `secrets set` nesta sessão, JWT novo autenticou normal, "favoritos" que não usa Gemini funcionou pro fundador); bug de lógica no fix anterior (o código faz exatamente o que devia, só a premissa de tempo estava errada). Recomendação registrada sem implementar: confirmar se é transitório (como o incidente de 23/08, que se resolveu sozinho); decisão estratégica de billing (RELATÓRIO 20260824_0002) ficou mais urgente; redesenho pra padrão assíncrono seria a solução arquitetural de verdade, não um fix pontual. |
| [20260825_0005_troca_modelo_foto_prato_flash_para_flash_lite.md](20260825_0005_troca_modelo_foto_prato_flash_para_flash_lite.md) | 2026-08-25 | Seguindo a recomendação do RELATÓRIO 20260825_0004 (arquiteto sênior, baixo custo), fundador aprovou explicitamente: "vamos então trocar o modelo de flash para flash lite". `NIVEL_POR_TIPO[pratoRefeicao]`: `core`→`lite` — Método 4 (foto) passa a usar o mesmo `gemini-flash-lite-latest` que texto/áudio já usam. `TIPO_ROTULO` (OCR de rótulo nutricional) não foi tocado, continua CORE (dígito de macro errado importa mais que item esquecido no prato). Fallback automático deixa de se aplicar a `pratoRefeicao` (mesmo comportamento que texto/áudio: sem fallback quando já se está no nível mais barato). 3 testes atualizados/divididos pro novo roteamento; Deno 100/100. Edge Function deployada e **confirmada ao vivo** contra o endpoint de produção (usuário de teste descartável): 2 fotos reais, 200 OK em 72,9s e depois 11,7s — melhora real e substancial vs. os 141,7s/150,8s do CORE, mas reportado sem inflar: não é instantâneo nem 100% estável ainda, o Gemini como um todo mostrou alguma variação no dia, o Flash-Lite só é muito mais confiável. Nada verificado em device físico ainda. |
| [20260825_0006_relatorio_completo_bug_foto_para_segundo_agente.md](20260825_0006_relatorio_completo_bug_foto_para_segundo_agente.md) | 2026-08-25 | Fundador testou de novo no device: bug persiste mesmo após 20260825_0005, e verificou diretamente no Google AI Studio que o Gemini está saudável (sem instabilidade, sem cota estourada) — descartando o modelo como causa atual. Também descartou rede (mesmo sintoma em Wi-Fi e dados móveis) e descartou o padrão "1ª vs 2ª tentativa" (erro já ocorreu na 1ª também). Pediu relatório completo pra avaliação por um segundo agente de IA. **Auditoria de código do app (não do servidor, pela 1ª vez nesta série)**: achado estrutural real — dos 3 fluxos que chamam IA (foto/texto/áudio), só o de FOTO (`camera_capture_view.dart`) não tem nenhuma checagem `if (!mounted) return` antes de navegar após a chamada de rede longa (10-70+s); texto e áudio (`gravar_refeicao_page.dart`) têm em todos os pontos. Hipótese líder (não confirmada): o Android pode suspender/recriar a Activity durante a espera longa, e a navegação acontece contra uma árvore já descartada, sem lançar exceção nenhuma (por isso nenhum erro aparece, nem em build debug que já expõe detalhe técnico). Documento autocontido criado (histórico completo, código-fonte na íntegra dos arquivos relevantes, dados reais medidos, perguntas em aberto) pra entregar a outro agente — nenhuma alteração de código nesta tarefa, só documentação. Próximo passo real recomendado: capturar `adb logcat` no momento exato da falha. |
| [20260825_0007_instrumentacao_diagnostico_bug_foto.md](20260825_0007_instrumentacao_diagnostico_bug_foto.md) | 2026-08-25 | Fundador trouxe log real da Edge Function de uma execução onde o app "fechou a tela da foto e voltou pra anterior": boot→último log = 3,3s, zero erro/retry/fallback — confirmou que o servidor processou rápido e sem problema, então o bug é 100% do lado do app. Pediu tarefa estruturada em 2 etapas (via outro agente de IA, seguindo o relatório 20260825_0006): Etapa 1 só instrumenta (zero mudança de comportamento), Etapa 2 (correção) só depois de um log real reproduzido. **Etapa 1 entregue**: log de conclusão (duração/status/tamanho do corpo) na Edge Function via wrapper fino (`processarRequest` renomeado, corpo idêntico, `handleRequest` vira só o log); log entre `calcularPrato` e o resto do pipeline; `debugPrint` no cliente assim que a resposta HTTP chega (antes de qualquer parsing), imediatamente antes do `pushReplacement`, e no `initState`(novo)/`build` de `ConfirmacaoPratoPage`/construtor de `ConfirmacaoPratoController` (não é widget, sem `initState`/`build`). Catalogado TODO ponto de `Navigator.pop`/retorno antecipado em `confirmacao_prato_page.dart` — achado: o único pop de página inteira exige o usuário ter apertado "Confirmar", não bate com o sintoma (reforça que o bug está antes desta tela existir). 19/19 testes de `confirmacao_prato_page_test.dart` passaram (tarefa mencionava 11 — suíte cresceu desde então). Deno 100/100 inalterado. Edge Function deployada. Nenhuma correção aplicada — aguardando log real reproduzido (device + Edge Function, mesma execução) pra Etapa 2. |
| [20260827_0001_fix_navegacao_foto_prato_pushreplacement_silenciosa.md](20260827_0001_fix_navegacao_foto_prato_pushreplacement_silenciosa.md) | 2026-08-27 | Fundador trouxe o log pareado (device `adb logcat` + Edge Function, mesma execução) que a Etapa 2 exigia. **Achado decisivo**: do lado do device só apareceram 2 linhas — resposta HTTP 200 recebida, e "`_onStateChanged`: prato extraído com 8 itens — chamando pushReplacement (`mounted=true`)" — e NADA depois. Nenhum log de `ConfirmacaoPratoController`/`ConfirmacaoPratoPage.initState`/`.build` (instrumentados na Etapa 1) apareceu. Prova concreta (não suposição): `mounted` já era `true` no instante exato da chamada — a hipótese "Android desmontou a árvore durante a espera" (RELATÓRIO 20260825_0006) fica **descartada por evidência**; `pushReplacement` foi chamado mas a tela de destino nunca rodou `initState`, sem exceção nenhuma visível em lugar nenhum. Causa raiz exata segue sem 100% de certeza — a janela ficou muito mais estreita, não o mecanismo interno completo. Aplicadas as 3 correções pré-combinadas na Etapa 1: (1) checagem de `mounted` em `_onStateChanged` (higiene, mesmo padrão de `gravar_refeicao_page.dart`); (2) `pushReplacement`→`push` no fluxo de foto (câmera fica na pilha em vez de sumir se a rota nova falhar; confirmar faz pop das duas telas, cancelar reinicializa a câmera pra tentar de novo); (3) `Future` de `_capturar()` tratado com `.catchError` de verdade (não só `unawaited()`, que só silencia o lint). `flutter analyze` limpo, Flutter 427/427. Gap conhecido registrado: zero teste automatizado pra `CameraCaptureView`/`CameraCaptureController` (pré-existente, mockar `CameraController` do pacote `camera` não é trivial). `debugPrint`s de diagnóstico mantidos de propósito pra confirmar o fix no próximo teste real — instruções detalhadas de como interpretar o próximo log (se falhar de novo) incluídas no relatório. |
