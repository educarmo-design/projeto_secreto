# Log de Dev — 20260807_0001

**Tarefa:** N26 (Fase 0) — higienizar a pilha de branches começando pelo merge de
`feature/f10-step3-meal-confirmation`, garantir compilação sem erros, e inicializar
N25 (governança de log de desenvolvimento).

**Branch de trabalho:** `chore/log-dev-governanca-n25` (criada a partir de `main`).

---

## Decisões técnicas | motivo

1. **Não executei nenhum merge de `feature/f10-step3-meal-confirmation`.**
   Motivo: investigação de `git log`/`merge-base` (Parte 0 — investigar antes de
   presumir) mostrou que as três branches da pilha já estão 100% mescladas —
   `feature/f10-step3-meal-confirmation` (bf2cdb4) → `feature/f34-coleta-diaria-persistence`
   (b39350c) → `feature/d2-server-side-pii-crypto` (3df5976) — via o commit de merge
   `e7959cf` ("Merge chore/r10-r6-r15-build-prep"), datado de **2026-07-30 21:43 -03:00**.
   Confirmado com `git merge-base --is-ancestor` para as três branches contra `main`
   **e** contra `origin/main`: todas retornam `YES`. Refazer o merge teria sido
   redundante e arriscado (reintroduziria um merge commit sem propósito ou geraria
   conflito artificial). Tratei isso como o achado central da auditoria, não como
   falha da tarefa.
2. **Criei uma branch dedicada (`chore/log-dev-governanca-n25`) só para o
   commit de governança**, em vez de commitar direto na `main`. Motivo: a
   restrição explícita "proibido merge direto na main/produção sem autorização
   prévia" — mesmo sendo só um arquivo de doc, mantive o padrão de branch +
   aprovação do fundador antes de entrar na `main`.
3. **Não toquei nas alterações soltas já presentes no working tree** (deleções de
   `docs/*MESTRE*`, `docs/PROMPT_*` etc., e as pastas novas não-rastreadas
   `docs/especificacao/`, `docs/novo_adendo/`, `docs/auditoria/20260730.txt` e
   `20260801.txt`). Motivo: são uma reorganização manual pré-existente, sem
   relação com N26/N25, fora do escopo desta tarefa (HOLD). Investigação mostrou
   que os arquivos "deletados" da raiz de `docs/` têm cópias mais novas em
   `docs/especificacao/` — parece uma reorganização intencional do fundador, não
   um acidente, mas não é meu lugar decidir commitar ou descartar isso sem
   confirmação.

## Mudanças de infra/config

Nenhuma. Nenhum arquivo de config, `.env`, chave ou variável de ambiente foi
alterado.

## Entidades novas

- `docs/log_dev/INDICE.md` — índice do log de governança (N25).
- `docs/log_dev/20260807_0001.md` — este relatório.

Nenhuma entidade de código (classe, tabela, endpoint) foi criada — a tarefa era
de auditoria/governança, não de feature.

## Desvios da spec

- A spec ("[TAREFA]") pedia para *fazer o merge* de `feature/f10-step3-meal-confirmation`.
  Não havia merge a fazer — já estava feito. Documentei o desvio em vez de simular
  uma ação sem efeito real.
- A spec pedia "commit estruturado na branch correta" pressupondo que a branch
  correta seria onde o merge aconteceria. Como não houve merge, o commit real
  desta tarefa é só a inicialização do log — feito em branch própria, não na `main`.

## Problemas encontrados

Nenhum problema bloqueador. Dois pontos não-bloqueadores para o fundador revisar
quando tiver tempo (não fazem parte do escopo N26/N25):

- `flutter analyze` reportou 24 issues, todas `info`/`warning` de lint (uso de
  `const`, um campo não utilizado em `login_page.dart:60`, tipos privados em API
  pública em `dynamic_widget_factory.dart`). Nenhum erro de compilação.
- O working tree tem uma reorganização de `docs/` não commitada (ver "Decisões
  técnicas", item 3) — vale um commit próprio quando o fundador confirmar a
  intenção.

## Riscos mapeados

- **Risco de duplicar histórico:** se alguém tentar mesclar
  `feature/f10-step3-meal-confirmation` (ou `f34`/`d2-pii`) de novo sem checar
  `git merge-base --is-ancestor` antes, o Git vai aceitar sem reclamar (fast-forward
  vazio ou "already up to date"), mas pode confundir quem lê o histórico depois.
  Mitigação: este relatório documenta as evidências (commits e datas) para
  referência futura.
- **`main` local estava 24 commits à frente de `origin/main`, não publicados.**
  Não fiz push automaticamente — aguardei autorização explícita do fundador
  (política de main/produção). Autorização recebida e `git push origin main`
  executado como fast-forward puro (`54daa6f..c386ee1`, sem force, sem conflito).
- **Branches da pilha (`feature/f10-step3-meal-confirmation`, `feature/f34-*`,
  `feature/d2-*`) estavam "presas no tempo"**: eram 38 commits mais antigas que a
  `main` atual (que já reescreveu partes do mesmo código — ex.:
  `confirmacao_prato_page.dart` mudou 379 linhas depois do merge). Com
  autorização do fundador, as 3 foram deletadas local e remotamente via
  `git branch -d` (delete seguro, não `-D`) — o próprio Git confirmou que
  estavam 100% mescladas antes de aceitar a deleção, sem precisar forçar nada.

## Como o fundador testa

1. `flutter pub get` — ok, sem erro.
2. `flutter analyze` — 0 erros (só infos/warnings de lint, listados acima).
3. `flutter build apk --debug` — **executado e confirmado nesta sessão**:
   `√ Built build\app\outputs\flutter-apk\app-debug.apk` (Gradle `assembleDebug`,
   348,4s, exit code 0). Único aviso: 4 plugins (`device_info_plus`,
   `file_picker`, `health`, `workmanager_android`) ainda aplicam o Kotlin
   Gradle Plugin (KGP) old-style — não bloqueia hoje, mas o Flutter avisa que
   passará a bloquear em versão futura; não é algo para resolver nesta tarefa.
4. Fluxo funcional (critério de aceite): logar no app → acionar o fluxo de
   registro de refeição (câmera/prato) → confirmar que `ConfirmacaoPratoPage`
   carrega. Nota de arquitetura: essa tela **não é uma rota do GoRouter** — é
   empurrada via `Navigator.push`/`pushReplacement` a partir de
   `camera_capture_view.dart`, por design documentado no próprio
   `app_router.dart` (só telas completas de navegação viram rota nomeada;
   `ConfirmacaoPratoPage` é uma tela transicional dentro do fluxo da câmera).
   Isso já era assim antes desta tarefa — confirmei que a ligação
   `camera_capture_view.dart` → `ConfirmacaoPratoPage` segue intacta.

## Como tratou performance

Não se aplica — nenhuma mudança de código/produto nesta tarefa. `flutter analyze`
sinalizou alguns `prefer_const_constructors` pré-existentes (otimização de
rebuild), não introduzidos por mim.

## Estado das branches / ordem de merge

| Branch | Status |
|---|---|
| `feature/f10-step3-meal-confirmation` | ✅ Estava mesclada desde 2026-07-30; **deletada** (local + remota) em 2026-08-07 por autorização do fundador. |
| `feature/f34-coleta-diaria-persistence` | ✅ Estava mesclada desde 2026-07-30; **deletada** (local + remota) em 2026-08-07 por autorização do fundador. |
| `feature/d2-server-side-pii-crypto` | ✅ Estava mesclada desde 2026-07-30; **deletada** (local + remota) em 2026-08-07 por autorização do fundador. |
| `main` (local) | **Publicada** em `origin/main` em 2026-08-07 (`54daa6f..c386ee1`, fast-forward, sem force). `main` local e `origin/main` agora apontam para o mesmo commit (`c386ee1`). |
| `chore/log-dev-governanca-n25` (atual) | Commit da governança N25, ainda não mesclada em `main` — pendente de decisão do fundador (ver "Sugestão de próxima ação"). |

**Sugestão de próxima ação:** resta apenas decidir sobre (a) mesclar
`chore/log-dev-governanca-n25` em `main` (só adiciona `docs/log_dev/`, sem
risco de código); (b) o que fazer com a reorganização não commitada de `docs/`
(`docs/especificacao/`, `docs/novo_adendo/`, deleções da raiz de `docs/`) —
segue fora do escopo desta tarefa, intocada.
