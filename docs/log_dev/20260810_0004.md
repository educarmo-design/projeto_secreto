# 20260810_0004 — FC máxima + balança (inferência cruzada) + restrição regulatória F02

- **Tarefa**: engenharia de dados (migration) + regra de negócio em Dart, decisão do fundador a partir do spike de `docs/log_dev/20260810_0003.md`.
- **Branch**: `feature/n17-n18-telemetria-persistida` (mesma branch de toda a telemetria de wearables).
- **Restrição regulatória verbatim do prompt (PRESERVADA, não código novo algum a viola)**: "Fica ESTRITAMENTE PROIBIDA a implementação de detecção de anomalias cardíacas (eventos de FC fora do normal). Isso pertence ao Motor Clínico (F02), que está no Backlog. Devemos apenas gravar os valores absolutos."

## Migration

- `supabase/migrations/20260811130000_metricas_saude_fc_maxima_balanca.sql`, aplicada com sucesso via `npx supabase db push`.
- Nota de data: nome do arquivo usa `20260811`, mas a data real do sistema no momento da criação era `20260810` (confirmado via `date +%Y%m%d` ANTES de criar/aplicar o arquivo, ao contrário do episódio anterior em `20260810_0002`). Escolhida deliberadamente para ordenar depois do `20260811120000_metricas_saude_sono_granular.sql` (já aplicado, também mal-datado), preservando a ordem monotônica das migrations diante do drift de data já existente. Um arquivo anterior com nome `20260812120000...` foi criado por engano e apagado (`rm`) antes de ser aplicado/commitado, ao perceber o erro de data.
- `metricas_saude_diarias.fc_maxima int` — máximo bruto de `HealthDataType.HEART_RATE` do dia. Comentário SQL explícito: "não é limite clínico nem evento".
- `metricas_saude_diarias.agua_corporal numeric(5,2)` — de `HealthDataType.BODY_WATER_MASS`.
- `metricas_saude_diarias.imc numeric(4,1)` — direto de `HealthDataType.BODY_MASS_INDEX` quando disponível, senão inferido.
- `perfis_usuarios.altura_cm numeric(5,1)` — coluna nova (não existia nenhum campo de altura antes desta migration; confirmado lendo `20260706191827_core_schema.sql`). Nullable, sem UI de preenchimento ainda (fora do escopo desta tarefa).
- GRANT: nenhum novo necessário (GRANT é por tabela, ambas as tabelas já concedem select/insert/update ao dono via RLS).

## Código Dart

- `lib/features/dashboard/data/models/health_payload_model.dart`: novos campos `aguaCorporalKg`/`imc`; novos casos em `fromHealthDataType` para `BODY_WATER_MASS` e `BODY_MASS_INDEX`.
- `lib/features/dashboard/data/services/health_sync_service.dart`:
  - `todosOsTipos` ganha `BODY_WATER_MASS` e `BODY_MASS_INDEX`.
  - `_mesclarPorDia`: novo acumulador `maximaFcPorDia` — só `math.max` puro sobre o mesmo dado bruto que já alimenta a média de FC, sem lógica de limite/faixa/evento. `agua_corporal`/`imc` seguem o mesmo padrão "sobrescrever" (última leitura) dos demais campos de balança.
  - Novo método `_aplicarInferenciasCruzadas(usuarioId, linhas)`, chamado em `_lerEGravar` depois de `_mesclarPorDia` (precisa dos valores já consolidados por dia) e antes de `_enviarLinhas`:
    - `massaMagra = peso × (1 − percentual/100)` — só quando peso+percentual presentes e massa magra ausente.
    - `percentual = (1 − massaMagra/peso) × 100` — só quando peso+massa magra presentes e percentual ausente.
    - Nunca sobrescreve um valor que o Health Connect já mandou.
    - IMC: se `imc` já não veio pronto do merge e há peso, busca `perfis_usuarios.altura_cm` (novo método `_buscarAlturaMetros`, best-effort — `null` em erro/RLS/coluna vazia, não derruba o sync) **uma única vez por chamada de sync**, não uma vez por linha/dia — testado explicitamente (`verify(...).called(1)`).
  - **Achado regulatório documentado, NÃO corrigido nesta tarefa (fora do escopo/ARQUIVOS pedidos)**: doc comment novo adicionado diretamente acima de `_detectarEregistrarAnomalias` sinalizando que esse método pré-existente (Adendo v5.1, "Caixa Preta", não criado nesta conversa) já faz exatamente o que a restrição desta tarefa proíbe — detecção de evento/anomalia de FC fora de faixa, gravando em `eventos_anomalias_saude`. Nenhum código novo desta tarefa adiciona lógica de anomalia. Não removido/desligado unilateralmente — decisão de escopo maior (alimenta também o Módulo de Inteligência) que precisa de decisão explícita do fundador.

## Verificação

- `flutter analyze lib/features/dashboard`: 5 issues (baseline, todos info-level, nenhum novo).
- `flutter test test/features/dashboard/data/services/health_sync_service_test.dart`: 55/55 passando, incluindo 8 testes novos (fc_maxima, agua_corporal, imc direto, as duas direções de inferência cruzada, imc por altura, imc sem altura, altura buscada uma única vez).
- `flutter test test/features/dashboard/`: suíte completa, 78/78 passando.
- `npx supabase db push`: migration `20260811130000_metricas_saude_fc_maxima_balanca.sql` aplicada com sucesso no banco remoto.
- `flutter build apk --debug`: disparado em background para confirmar compilação real (ver nota de resultado no próximo log, se necessário, seguindo o padrão desta sessão de nunca confiar só no status "killed"/"running" sem checar o arquivo no disco).

## Commits

- Código: `fix(f10-telemetria): fc_maxima, agua_corporal, imc e inferência cruzada de balança (N17/N18)`.
- Log: commit separado `docs(log_dev): registra tarefa fc_maxima/balança/restrição F02`.
